直接插入算法顾名思义就是进行n-1次的插入合适位置，就比如小时候做体操排队，比身高之后去插入队伍，这是一个道理

显而易见这是一个稳定的排序方法

/**
 * 31542
 * 13542
 * 13542
 * 13452
 * 12345
 */
public static void mian(String[] args) {
    int[] demo = new int[3,1,5,4,2];
    for(int i = 1; i < demo.length; i++) {
        for(int j = 0; j < i; j++) {
            if(demo[i] < demo[j]) {
                demo[j] = demo[i];
            }
            demo[j+1] = demo[j];
        }
    }
}

这个时间复杂度和初始排序有关系，如果开始就是顺序的话，那么时间复杂度O(N),如果是逆序，那么时间复杂度就是O(N^2)
